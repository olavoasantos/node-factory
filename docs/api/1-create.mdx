---
name: create
route: /api/create
menu: API
---

import UserFactory from './UserFactory';

## `Factory.create([overrides])`

**args:**

- `overrides (IDataObject [optional])`: These are value which should be overridden on the generated data (default: `{}`)

**return:**

- `data (IDataObject)`: Data object generated by your generator function

### Description

This method is used to generate a data object.

### Example

```js
UserFactory.create()
```

<UserFactory parse={(UserFactory) => UserFactory.create()} />

```js
UserFactory.create({ name: 'JOHN DOE' })
```

<UserFactory parse={(UserFactory) => UserFactory.create({ name: 'JOHN DOE' })} />
