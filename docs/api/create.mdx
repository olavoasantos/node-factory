---
name: create
route: /api/create
menu: API
---

import { Link } from 'docz';
import FormatJson from '../components/FormatJson';
import { UserFactory } from '../components/Factory';

# `Factory.create([overrides])`

### args

- `overrides `<Link to="/api/types#idataobject">`(IDataObject [optional])`</Link>: These are value which should be overridden on the generated data `(default: {})`

### return

- `data `<Link to="/api/types#idataobject">`(IDataObject)`</Link>: Data object generated by your generator function

## Description

This method is used to generate a data object.

## Example

```js
UserFactory.create()
```

<FormatJson parse={() => UserFactory.create()} />

```js
UserFactory.create({ name: 'JOHN DOE' })
```

<FormatJson parse={() => UserFactory.create({ name: 'JOHN DOE' })} />
