---
name: only
route: /api/only
menu: API
---

import UserFactory from './UserFactory';

## `Factory.only(fields[, overrides])`

**args:**

- `fields (String | Array)`: This argument defines the fields that should be returned.

> - If a `String`, returns an object containing only that key <br />
> - If an `Array`, returns an object containing only the keys contained in the array

- `overrides (IDataObject [optional])`: These are value which should be overridden on the generated data (default: `{}`)

**return:**

- `data (IDataObject)`: Data object generated by your generator function

### Description

This method is used to generate a data object containing only the keys specified. This could be particularly useful for updating state.

### Example

```js
UserFactory.only('email')
```

<UserFactory parse={(UserFactory) => UserFactory.only('email')} />

```js
UserFactory.only(['email', 'name'])
```

<UserFactory parse={(UserFactory) => UserFactory.only(['email', 'name'])} />

```js
UserFactory.only(['id', 'email'], { id: '123' })
```

<UserFactory parse={(UserFactory) => UserFactory.only(['id', 'email'], { id: '123' })} />
